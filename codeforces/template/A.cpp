#include <bits/stdc++.h>
using namespace std;

// COMPILER: GNU C++17	

int main() {
    int t;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        int n;
        cin >>n;

        vector<int> vec(n);
        for (int j = 0; j < n; ++j) {
            cin >> vec[j];
        }

        for (int k = 0; k < n; ++k) {
            // print code!
        }
        cout << '\n';
    }
}   